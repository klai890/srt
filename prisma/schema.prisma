// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL") // Only needed when using a cloud provider that doesn't support the creation of new databases, like Heroku. Learn more: https://pris.ly/migrate-shadow
}

generator client {
  provider        = "prisma-client-js"
}

model Account {
  id                 String  @id @default(cuid())
  userId             String
  type               String
  provider           String
  providerAccountId  String
  refresh_token      String?  @db.Text
  access_token       String?  @db.Text
  expires_at         Int?
  token_type         String?
  scope              String?
  id_token           String?  @db.Text
  session_state      String?
  athlete            Json?

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
}

// type Athlete {
//   id                 Int?    @map("_id") // but already included in Account, so might not even care
//   firstname          String?
//   lastname           String?

  // prob don't care much abt these
  // bio                String?
  // city               String?
  // state              String?
  // country            String?
  // sex                String?
  // premium            String?
  // summit             String?
  // created_at         String?
  // updated_at         String?
  // badge_type_id      Int?
  // weight             Float?
  // profile_medium     String?
  // profile            String?
  // friend             String?
  // follower           String?

  // id: 95300802,
  // username: null,
  // resource_state: 2,
  // firstname: 'Goose',
  // lastname: 'Se',
  // bio: 'Lives in Seattle',
  // city: 'Cupertino',
  // state: 'California',
  // country: 'United States',
  // sex: 'F',
  // premium: true,
  // summit: true,
  // created_at: '2021-11-13T15:58:21Z',
  // updated_at: '2022-11-15T04:12:58Z',
  // badge_type_id: 1,
  // weight: 52.1631,
  // profile_medium: 'https://dgalywyr863hv.cloudfront.net/pictures/athletes/95300802/22587571/1/medium.jpg',
  // profile: 'https://dgalywyr863hv.cloudfront.net/pictures/athletes/95300802/22587571/1/large.jpg',
  // friend: null,
  // follower: null
// }


model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model User {
  id            String    @id @default(cuid())
  name          String?
  email         String?   @unique
  emailVerified DateTime?
  image         String?
  accounts      Account[]
  sessions      Session[]
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
}